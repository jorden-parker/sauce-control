# Implementation Plans

Generated by the improve skill on 2026-09-11 (`plan` variant, no audit run). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title                                                               | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Store the GitHub request log in SQLite instead of a JSON-lines file | P2       | S      | —          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None yet.

## Findings considered and rejected

- Putting the request table inside `settings.db`: rejected in plan 001's design notes; two modules sharing one connection-less file invites `SQLITE_BUSY` handling for no gain.
- Keeping the JSON-lines file alongside the database: rejected; the user asked for a replacement, and two sinks drift.
